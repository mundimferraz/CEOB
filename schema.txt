
-- ==========================================
-- SCRIPT DE ESTRUTURA SGR-VIAS ATUALIZADO
-- ==========================================

-- 1. TABELA DE USUÁRIOS ... (já existente)

-- 2. TABELA DE ZONAIS ... (já existente)

-- 3. TABELA DE VISTORIAS ... (já existente)

-- 4. TABELA DE ROTEIROS DE VISITAS (COPIE ESTA PARTE SE O RESTO JÁ EXISTIR)
CREATE TABLE IF NOT EXISTS visit_routes (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    technician_id TEXT REFERENCES users(id) ON DELETE CASCADE,
    request_ids TEXT[] NOT NULL,
    status TEXT DEFAULT 'Pendente',
    created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL
);

ALTER TABLE visit_routes DISABLE ROW LEVEL SECURITY;
